<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
        DEBUG ONLY: cloud AI (Tono) is development-only for now, so only debug builds may reach the
        network. Release builds have no INTERNET permission and no API key; the coach runs offline.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
